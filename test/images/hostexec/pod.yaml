apiVersion: v1
kind: Pod
metadata:
  name: hostexec
  labels:
    app: hostexec
spec:
  containers:
  - name: hostexec
    image: gcr.io/kubernetes-e2e-test-images/hostexec:1.2-linux-amd64
  securityContext:
    hostNetwork: true
